@model User
@{ Layout = "_Layout"; ViewBag.Title = "Add a User";}

<h2>Add a User <a asp-action="Index" class="btn btn-sm btn-secondary">Back</a></h2>
<form asp-action="AddUser" method="post">
    @*<div class="form-group">
        <label for="Name">Name:</label>
        <input class="form-control" name="Name" />
    </div>
    <div class="form-group">
        <label for="StartLocation">Start Location:</label>
        <input class="form-control" name="StartLocation" />
    </div>
    <div class="form-group">
        <label for="EndLocation">End Location:</label>
        <input class="form-control" name="EndLocation" />
    </div>
    <div class="text-center panel-body">
        <button type="submit" class="btn btn-sm btn-primary">Add</button>
    </div>*@
    
    <div class="form-group">
        <label for ="FirstName">First Name:</label>
        <input class="form-control" name="FirstName" />
    </div>

    <div class="form-group">
        <label for="LastName">Last Name:</label>
        <input class="form-control" name="LastName" />
    </div>

    <div class="form-group">
        <label for="Email">Email:</label>
        <input class="form-control" name="Email" />
    </div>

    <div class="form-group">
        <label for="Phone">Phone</label>
        <input class="form-control" name="Phone" />
    </div>

    <div class="form-group">
        <label for="Gender">Gender:</label>
        <input class="form-control" name="Gender" />
    </div>

    <div class="text-center panel-body">
        <button type="submit" class="btn btn-sm btn-primary">Add</button>
    </div>

</form>

<h3 class="alert">@ViewBag.Result</h3>

@if (Model != null)
{
    <h2>User</h2>
    <table class="table table-sm table-striped table-bordered m-2">
        <thead>
            <tr>
                <th>FirstName</th>
                <th>LastName</th>
                <th>Email</th>
                <th>Phone</th>
                <th>Gender</th>
            </tr>
        </thead>
        <tbody>
            <tr>
                <td>@Model.name.first</td>
                <td>@Model.name.last</td>
                <td>@Model.email</td>
                <td>@Model.phone</td>
                <td>@Model.gender</td>
            </tr>
        </tbody>
    </table>
}